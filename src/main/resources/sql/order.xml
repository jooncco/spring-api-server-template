<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.templates.springapiserver.order.repository.OrderRepository">
    <select id="getOrders" parameterType="int" resultType="Order">
    <![CDATA[
        SELECT  ORDER_ID, MEMBER_ID, ORDER_NO, ORDER_TYPE_CODE, ORDER_STATUS_CODE, ITEM_TOTAL_AMOUNT,
                DELIVERY_FEE_AMOUNT, ORDER_TOTAL_AMOUNT, CREATED_DATE_TIME, CREATED_BY, UPDATED_DATE_TIME, UPDATED_BY
          FROM  `ORDER`
         WHERE  MEMBER_ID = #{memberId}
    ]]>
    </select>
</mapper>